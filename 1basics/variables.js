// 1) you can't define variable more than once
let petName = 'Sharlotta';
petName = 'Lotta';
console.log(petName); 

// 2) there are rules related to the variable name: (a-Z, $, _)
let a1, $b, _c, camelCase, under_score;

// 3) variable name can't be reserved keywords
